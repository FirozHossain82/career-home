import React from 'react';

const Jobs = ({job}) => {
    const {logo,title,company_name,job_type,job_type1,job_location,salary} = job;
    return (
        <div>
            <h1>This is jobs category</h1>
        </div>
    );
};

export default Jobs;